package com.example.gesioncompteom.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerViewConfig {
    // No custom forwards anymore: springdoc will serve the UI and assets under the configured path
}
