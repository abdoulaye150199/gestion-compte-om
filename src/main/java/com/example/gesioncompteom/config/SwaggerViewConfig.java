package com.example.gesioncompteom.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerViewConfig {
    // Intentionally empty to avoid mapping conflicts with springdoc
}
